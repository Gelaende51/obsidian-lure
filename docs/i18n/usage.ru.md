<!-- Перевод docs/usage.md — состояние: коммит 7b2691a.
     Машинный перевод (Claude Opus 5), не вычитан носителями языка.
     Подписи плагина взяты из src/lang/translations.ts, а подписи Obsidian —
     из строк, которые поставляет само приложение, так что они совпадают с
     тем, что вы видите на экране. -->

**Читать на других языках:** [English](../usage.md) · [العربية](usage.ar.md) · [አማርኛ](usage.am.md) · [Беларуская](usage.be.md) · [বাংলা](usage.bn.md) · [Català](usage.ca.md) · [Čeština](usage.cs.md) · [Dansk](usage.da.md) · [Deutsch](usage.de.md) · [Ελληνικά](usage.el.md) · [Español](usage.es.md) · [فارسی](usage.fa.md) · [Suomi](usage.fi.md) · [Français](usage.fr.md) · [Gaeilge](usage.ga.md) · [עברית](usage.he.md) · [Magyar](usage.hu.md) · [Bahasa Indonesia](usage.id.md) · [Italiano](usage.it.md) · [日本語](usage.ja.md) · [ქართული](usage.ka.md) · [ភាសាខ្មែរ](usage.kh.md) · [한국어](usage.ko.md) · [Latviešu](usage.lv.md) · [Bahasa Melayu](usage.ms.md) · [नेपाली](usage.ne.md) · [Nederlands](usage.nl.md) · [Norsk](usage.no.md) · [Polski](usage.pl.md) · [Português](usage.pt.md) · [Português (Brasil)](usage.pt-BR.md) · [Română](usage.ro.md) · **Русский** · [संस्कृतम्](usage.sa.md) · [Slovenčina](usage.sk.md) · [Shqip](usage.sq.md) · [Српски](usage.sr.md) · [Svenska](usage.sv.md) · [ไทย](usage.th.md) · [Türkçe](usage.tr.md) · [Українська](usage.uk.md) · [Oʻzbekcha](usage.uz.md) · [Tiếng Việt](usage.vi.md) · [简体中文](usage.zh.md) · [繁體中文](usage.zh-TW.md)

# Использование

[← назад к README](README.ru.md)

## Строка пути

Полный путь заметки внутри хранилища заменяет голое имя файла в шапке представления — на панели под рядом вкладок, той самой, где находятся кнопки «назад» и «вперёд».

В строке кликабельны две вещи, и **Имя папки открывает список** решает, что из них что делает:

| | Имя папки | Разделитель после него |
| --- | --- | --- |
| **Включено** (по умолчанию) | Выделяет эту папку для редактирования | Открывает папку |
| **Выключено** | Открывает папку | Спускается в эту папку |

«Открывает папку» значит то, что делает этот щелчок в чистом Obsidian. Если там никто не слушает, папка показывается в боковой панели Файлового менеджера — подсвеченной и раскрытой, чтобы было видно содержимое.

С установленным [Folder notes](obsidian://show-plugin?id=folder-notes) тот же щелчок открывает вместо этого заметку той папки. Это единственный плагин заметок-папок, замеченный в том, что он занимает путь в шапке; [Folder Note](obsidian://show-plugin?id=folder-note-plugin) и [create folder notes with dropdown](obsidian://show-plugin?id=create-folder-notes-with-dropdown) управляют заметками папок, но щелчок по пути не слушают, так что с ними разделитель показывает папку как обычно. См. [совместимость](../compatibility.md#verified-against).

Разделитель **подчёркнут только тогда, когда у папки перед ним действительно есть заметка-папка**, так что подчёркивание — обещание, что там есть что открыть. Кликабельным остаётся при этом любой разделитель — тот, что без подчёркивания, показывает и раскрывает свою папку в боковой панели, о чём по-прежнему сообщает курсор-рука. Одновременно подчёркивание уходит с имени папки: при включённой смене имя открывает список, так что помечать его как ссылку на заметку было бы ложью.

**Режим переименования/перемещения главнее обоих**, что бы ни говорила настройка: пока перемещение не завершено, ничто в строке не открывает папку, потому что открытие бросило бы перемещение. Имена папок выделяются для правки, а разделители спускаются вниз — и то и другое способ выбрать цель — и подчёркивание исчезает, показывая, что открытие приостановлено.

**Корень хранилища** — единственный сегмент, который не является сегментом пути. У него нет родительской папки, из которой можно перечислить соседей, поэтому он открывает вместо этого [список расположений](#просмотр-за-пределами-хранилища) — ваши другие хранилища, домашнюю папку, корень файловой системы и подключённые диски.

## Щелчок по сегменту: замените его на соседний

Щелчок по имени папки выделяет **имя этой папки** в текстовом поле и открывает список папки **уровнем выше** — её родительской. Ввод или выбор записи меняет эту папку на соседнюю и оставляет нетронутым всё, что ниже, так что `Проекты/2026/Старт.md` → щелчок по `2026` → выбрать `2025` даёт `Проекты/2025/Старт.md`.

Щелчок по **имени заметки** работает так же относительно её собственной папки и выделяет имя файла **вместе с расширением** — переименование или перенацеливание заметки обычно означает изменить и его.

Щелчок по папке уже выделил один сегмент, поэтому **ещё один щелчок** расширяет выделение на всю строку — эту папку *и* всё, что ниже, — и то, что вы наберёте, заменит тогда остаток пути разом. Работает одинаково в навигации и в режиме переименования/перемещения.

Это работает только как продолжение щелчка, открывшего поле. Как только вы поработали с полем, оно ведёт себя как любое другое текстовое поле: щелчок ставит курсор, двойной берёт слово, тройной берёт строку.

В любом случае остаток пути остаётся видимым вокруг поля — как фишки перед ним и как невыделенный текст после, — так что полный путь никогда не исчезает из шапки. Печатайте, чтобы заменить выделение, или нажмите <kbd>End</kbd> / <kbd>→</kbd>, чтобы сохранить его и править дальше отсюда. Список показывает папку целиком независимо от того, что подставлено; фильтровать он начинает лишь тогда, когда вы действительно печатаете.

## Спуск по разделителю

Щелчок по разделителю (при выключенном **Имя папки открывает список**) спускает в папку перед ним: список показывает содержимое *этой* папки, а остаток пути открывается выделенным в поле. Выбор папки добавляет её к цепочке пути и сразу открывает следующий список, так что можно спускаться по дереву щелчками, не покидая строки шапки.

## Записи списка — настоящие строки файлового менеджера

Каждый файл и папка в списке ведёт себя как своя строка в Файловом менеджере:

- **Правый щелчок** даёт то же контекстное меню — *Новая заметка* / *Новая папка* на папке, *Открыть в новой вкладке* / *Переименовать* / *Удалить* на файле, — включая пункты, которые добавляют в файловые меню другие плагины.
- **Перетащите** запись куда угодно, где Obsidian принимает файл: в редактор, чтобы вставить ссылку, на папку в Файловом менеджере, чтобы переместить, на панель вкладок, чтобы открыть.

Текст меню берётся из собственных переводов Obsidian, так что он совпадает с остальной частью приложения на любом языке.

## Ввод пути

- Щелчок по **пустому месту** до или после пути открывает текстовое поле, заполненное всем путём и полностью выделенное — печатайте поверх или правьте на месте. (Щелчок по самому имени файла выделяет только имя; см. выше.)
- Набор текста, когда видна цепочка пути, превращает последний сегмент в маленькое поле с живым автодополнением в пределах текущей папки.
- `/` подтверждает текущий сегмент и спускается в него.
- <kbd>Backspace</kbd> в пустом поле возвращает в родительскую папку, снова открывая её имя с курсором в конце.
- <kbd>Enter</kbd> подтверждает; <kbd>Esc</kbd> или щелчок в другом месте отменяет и возвращает к настоящему пути файла.

У поля нет обрамления — ни рамки, ни границы, — так что оно читается как сам текст пути и само растёт по мере набора.

## Навигация никогда не трогает открытый файл

В режиме по умолчанию (навигации) открытая заметка **никогда** не переименовывается и не перемещается.

- Путь, ведущий к существующему файлу, открывает его.
- Путь, которого ещё нет, спрашивает *«Создать новый файл?»*. Подтверждение создаёт недостающие папки и файл; отмена не делает ровным счётом ничего.

## <kbd>Ctrl</kbd> — новая вкладка и копирование вместо перемещения

Удержание <kbd>Ctrl</kbd> (<kbd>Cmd</kbd> в macOS) при выборе файла из списка или при нажатии <kbd>Enter</kbd> на пути отправляет результат в **новую вкладку**, а не в эту:

| | Просто | С <kbd>Ctrl</kbd> |
| --- | --- | --- |
| Выбрать или ввести существующий файл | Открывается здесь | Открывается в новой вкладке |
| Ввести несуществующий путь | Спрашивает, затем открывает здесь | Спрашивает, затем открывает в новой вкладке |
| Подтвердить путь в режиме переименования/перемещения | **Перемещает** заметку туда | **Копирует** её туда и открывает копию в новой вкладке |

Модификатор считывается по правилу самого Obsidian, так что ведёт себя ровно как на ссылке или на строке Файлового менеджера — средний щелчок тоже значит «новая вкладка», <kbd>Ctrl</kbd>+<kbd>Alt</kbd> значит разделение, а <kbd>Ctrl</kbd>+<kbd>Alt</kbd>+<kbd>Shift</kbd> — новое окно.

Копирование отказывается перезаписывать, ровно как и перемещение — в том числе на собственный путь заметки, где копировать попросту нечего.

## Просмотр за пределами хранилища

**По умолчанию это выключено.** Сначала включите **Доступ к внешним файлам** в настройках — чтение и запись за пределами хранилища единственное, что делает этот плагин и чего сам Obsidian не сделает, поэтому в это входят намеренно, а не выходят. При выключенной настройке имя хранилища просто показывает ваше хранилище в Файловом менеджере, и ничто здесь никогда не заглядывает дальше.

Щелчок по **имени хранилища** (или по значку 🏠, когда *Показывать имя хранилища* выключено) открывает список мест, а не содержимого:

- **Ваши другие хранилища**, прочитанные из собственного реестра Obsidian, сначала открывавшееся последним, каждое под собственным значком хранилища Obsidian — тем же, который приложение использует для команд, связанных с хранилищами. У хранилища, которое уже открыто, вместо этого домик: это то, откуда строка начинается по умолчанию, а не место, куда можно пойти.
- **Домашняя папка**, под именем вашей учётной записи, помеченная `~`. В Lucide нет тильды, поэтому этот значок рисует сам плагин на той же сетке 24×24 Lucide и с той же толщиной штриха — значок, которого набору не хватает, а не текстовый символ, затесавшийся среди значков.
- **Корень файловой системы**, подписанный `root` — без перевода, потому что так он называется в любой системе, — а не `/`, который рядом со следующим за ним разделителем читался бы как пустой шаг.
- **Подключённые диски**, со значком по типу там, где это дёшево определить: сетевые ресурсы, оптические диски, дискеты и съёмные носители получают свой; всё прочее получает обобщённый диск. В Windows диски показываются как `C:` с обобщённым значком — имена томов и точные типы требуют WMI, который намеренно не используется.

Выбор другого хранилища **не переключает на него Obsidian.** Всё, что у вас открыто, остаётся открытым; строка пути просто начинает просмотр там. В этом весь смысл того, что оно на строке пути, а не отдано переключателю хранилищ в боковой панели.

### Пока вы снаружи

Путь **начинается с выбранного вами места**, а не с устройства каталогов машины — выберите `Архив`, и строка читается как `Архив / заметки / …`, а не `/home/вы/Vaults/Архив/заметки/…`. Первый сегмент несёт значок того, что это (хранилище, домашняя папка, диск), а <kbd>Backspace</kbd> останавливается на нём, а не идёт дальше вверх по остальной файловой системе. При выключенном *Показывать имя хранилища* этот сегмент — один только значок: настройка про начальный сегмент строки, какое бы хранилище он ни называл, а не только про ваше.

Строка пути остаётся **в рамке цвета ошибки** — том же кольце, которое рисует режим переименования, — всё время, пока указывает за пределы вашего хранилища. Она отмечает длящееся состояние, а не миг: пока она есть, ничто из собственной обработки Obsidian не относится к тому, что показывает строка, а запись заблокирована, пока вы не скажете иначе.

В остальном просмотр работает как внутри: фишки, разделители, ввод, автодополнение, <kbd>Backspace</kbd> для выхода. Действуют и те же правила видимости, так что неподдерживаемым расширениям по-прежнему нужно **Обнаружение файлов всех расширений** Obsidian, а скрытым файлам — настройка этого плагина.

**Правый щелчок и перетаскивание** по записям списка там не работают — это собственные обработчики Файлового менеджера, и им нужен файл, который хранилище знает.

### Запись за пределами хранилища

Всё, что пишет, **по умолчанию заблокировано**. Рядом с переключателем переименования в шапке появляется **замок** всё время, пока строка указывает за пределы вашего хранилища; нажатие открывает замок и красит его в красный, в тон кольцу вокруг строки.

Разрешение даётся **месту, а не мгновению**: оно переживает всё, что вы делали бы, работая в одном месте — завершение перемещения, щелчок мимо поля, открытие файла, — и заканчивается, когда вы выбираете из списка другое хранилище, диск или корень, когда строка возвращается к файлу хранилища или когда вы нажимаете замок ещё раз. Так серия перемещений внутри одной папки стоит одного нажатия, а не одного на файл.

С открытым замком строка пути ведёт себя там так же, как внутри:

| Действие | Результат |
| --- | --- |
| Ввести несуществующее имя, <kbd>Enter</kbd> | Тот же вопрос «создать?», что и внутри; недостающие папки тоже создаются. Имя без расширения становится `.md`, ровно как внутри |
| Режим переименования/перемещения, ввести новое имя | Переименовывает файл, который показывает строка. Имя без расширения сохраняет расширение файла — здесь снаружи папка держит файлы любого рода, и переименование не должно молча превращать `.png` в `.md` |
| Режим переименования/перемещения, перейти в другое место, выбрать **оставить это имя** | Перемещает туда под тем именем, которое уже есть |
| Удержать <kbd>Ctrl</kbd> на любом из двух | Копирует вместо перемещения и открывает копию в новой вкладке |

С закрытым замком всё это вместо действия сообщает, что его блокирует. Ни в одном из состояний ничего не перезаписывается: уже существующая цель отклоняется, и отказ исходит от самой файловой системы (`COPYFILE_EXCL`, исключительное создание), а не от проверки, которая могла бы проиграть гонку. Перемещение между файловыми системами — с флешки, с сетевого ресурса — переходит на «скопировать, затем удалить», и оригинал убирается только после того, как копия легла на место.

**Одного замок не разблокирует: перемещения заметки *за пределы* вашего хранилища.** `fileManager` не может проследить за файлом через эту границу, поэтому каждая ссылка на заметку молча сломалась бы, а Obsidian просто увидел бы, что она исчезла. Удержание <kbd>Ctrl</kbd> копирует её наружу, где этой беды нет, и уведомление так и говорит. Обратное направление — втянуть внешний файл *в* хранилище — тоже пока не сделано.

### Открытие внешнего файла

Редактор Obsidian работает только с файлами внутри хранилища, поэтому внешний файл **нельзя** открыть как настоящую заметку со ссылками, обратными ссылками и всем прочим — это ограничение приложения, а не плагина. Выбор такого файла открывает вместо этого **просмотр**, только для чтения, пока вы не скажете иначе:

| Тип | Показывается как |
| --- | --- |
| `.md`, `.markdown` | Отрисованный Markdown |
| Изображения, аудио, видео, PDF | Родной проигрыватель/просмотрщик |
| Любой другой **текстовый** файл (`.json`, `.css`, `.log`, `.txt`, …) | Простой текст как есть |
| Двоичные форматы без просмотрщика (`.zip`, `.exe`, …) | Передаются в *Открыть во внешней программе* |

У просмотрщика два прочтения файла, и, поскольку они исключают друг друга, показывается только то, **на которое** вы бы переключились:

| | Что делает | По умолчанию для |
| --- | --- | --- |
| **Показать как Markdown** | Отрисовывает файл как заметку, только для чтения | `.md`, `.markdown` |
| **Править как текст** | Исходный текст, редактируемый | всё остальное |

За пределами хранилища **Править как текст** — это ещё и то нажатие, которое снимает «только чтение»: режим и разрешение оказываются одним жестом, а не двумя кнопками, о которых надо рассуждать. Кнопка окрашивается красным **всякий раз, когда нажатие сняло бы «только чтение»**, взводите ли вы правку на месте или пришли прямиком из отрисованного вида; внутри хранилища разблокировать нечего, поэтому она остаётся обычной. **Показать как Markdown** получает лёгкую заливку цветом акцента — тот же оттенок, которым Obsidian отмечает выделенный текст, — обозначая её как путь назад, а не как призыв к действию.

Поскольку кнопка следит за *правкой*, а не за сырым режимом, файл, лежащий в текстовом виде только для чтения, всё равно предлагает **Править как текст**: это и есть то нажатие, которое её взводит. Файл, в который никогда нельзя будет писать — обрезанный или нечитаемый, — говорит вместо этого **Показать как текст**, потому что это всё, что нажатие может дать.

Значения по умолчанию выбраны полезные, а не буквальные: `#` в сценарии оболочки — это комментарий, а не заголовок, так что отрисовка `.log` как Markdown молча его проглотила бы. Любое из умолчаний можно переопределить для отдельного файла, и выбор уходит в историю вкладки, поэтому «назад/вперёд» и заново открытое рабочее пространство его сохраняют — множество заметок живёт в файлах `.txt`, и множество файлов `.md` читается легче как исходный текст.

**Файлы в вашем хранилище редактируемы сразу**, безо всякой разблокировки: *Править как текст* — настоящий редактор, и он пишет по мере набора.

**Правка запоминается при переключении.** Переход в *Показать как Markdown* её приостанавливает — в статичной отрисовке негде печатать, а «Живой просмотр» требует собственного редактора Obsidian, который существует только для файлов внутри хранилища, — так что ничто не утверждает, будто вы правите, пока вы там. Возврат к *Править как текст* подхватывает с того места, где вы остановились.

**Файлы за пределами хранилища открываются только для чтения, и *Править как текст* это снимает.** Это нажатие и есть весь заслон: пока его нет, снаружи ничего не пишется. Дальше файл сохраняется по мере набора, ровно как файл из хранилища; а строка состояния меняет замок на карандаш. Разблокировка распространяется на этот один файл в этой одной вкладке — переход к другому файлу снова блокирует, и она намеренно не сохраняется в истории вкладки, чтобы заново открытое рабочее пространство никогда не вернулось со взведённой записью на системном файле, об открытии которого вы не помните.

**Обрезанные файлы в любом случае остаются только для чтения** — сохранение того, что на экране, выбросило бы всё за пределом, поэтому кнопка вовсе не предлагается, вместо того чтобы предлагаться и отказывать. То же и с файлом, который не удалось прочитать: записывать обратно нечего, кроме пустой панели.

Если запись не удалась — раздел, смонтированный только для чтения, чужой файл, — причина, названная самой системой, показывается в уведомлении.

Очень большие файлы показываются обрезанными, и строка состояния так и говорит, а не оставляет вам это выяснять — рядом с прочими условиями, а не хвостом за кнопками, потому что это такой же факт о файле, как и остальные. Пределы измерены на настоящем отрисовщике, а не угаданы: разложить мегабайт текста в одной панели наповал убивает процесс отрисовки Obsidian, а Markdown стоит в несколько раз дороже на байт, чем простой текст, поэтому у них отдельные пределы, и одна огромная строка укорачивается, даже когда файл целиком невелик.

**Строки состояния — это ярлыки, а объяснение живёт во всплывающей подсказке.** Каждая строка говорит, что верно, в столь немногих словах, сколько нужно — *Вне хранилища*, *Нет редактора для этого типа файлов*, *Обрезано — файл слишком большой*, — потому что кнопки рядом уже говорят, в каком состоянии файл. Наведение даёт фразу: почему Obsidian не может открыть его как заметку, что иначе случилось бы с этим типом файла, чего стоит обрезание.

Это относится и к файлам **внутри** вашего хранилища. Любое расширение, для которого у Obsidian нет представления, он передаёт прямо в приложение рабочего стола по умолчанию — так что `.txt` или `.json` в вашем хранилище вывел бы вас из Obsidian целиком. Теперь такие открываются в том же просмотрщике, с оранжевым кольцом, потому что «открой это в Obsidian» и есть то, о чём вы просили, — а будучи файлами хранилища, они там редактируемы безо всякой разблокировки. Двоичные файлы без просмотрщика сохраняют поведение Obsidian; показывать нечего.

Просмотр открывается **в той вкладке, где вы были**, поэтому «назад/вперёд» возвращают к заметке, откуда вы пришли; удерживайте <kbd>Ctrl</kbd> для новой вкладки, как и везде. Панель шапки продолжает показывать путь внешнего файла, пока он открыт, так что оттуда можно продолжать просмотр.

Негромкая строка над содержимым предлагает выходы:

- **Открыть в «*(хранилище)*»** — показывается, когда файл принадлежит одному из ваших других хранилищ. Передаёт его собственному обработчику URI Obsidian, который открывает окно того хранилища с заметкой внутри, как настоящую редактируемую заметку. Это окно остаётся ровно таким, каким было; ничто не переключается у вас под руками.
- **Показать как Markdown** / **Править как текст** — два прочтения; второе снаружи хранилища ещё и снимает «только чтение».
- **Открыть во внешней программе** — передаёт файл приложению вашего рабочего стола по умолчанию, включая двоичные форматы, которые этот просмотрщик показать не может.

Ничто за пределами вашего хранилища не записывается, пока вы не нажмёте *Править как текст*. Полное раскрытие — в разделе [За пределами хранилища](README.ru.md#за-пределами-хранилища) README.

## Два цвета предупреждений

| | Когда | Что значит |
| --- | --- | --- |
| **Красное** кольцо на строке пути | Строка указывает за пределы вашего хранилища | Obsidian не может открыть то, что там, как заметку, и снаружи ничего не пишется, пока вы не откроете замок. |
| **Оранжевое** кольцо на строке пути, оранжевые записи в списке | Файл текстового типа, для которого у Obsidian нет представления | Предостережение. Obsidian передал бы его приложению вашего рабочего стола по умолчанию; плагин показывает его сам. |

**Оба независимы, и оба могут действовать разом** — внешний `.json` находится вне вашего хранилища *и* относится к типу, для которого у Obsidian нет редактора. В просмотрщике они выглядят как отдельные строки, каждая сообщает только свой факт. На строке пути, где действуют оба, побеждает красный, потому что два кольца были бы просто шумом.

Оранжевая ступень намеренно узка. Зарегистрированные типы (Markdown, канва, изображения, PDF, аудио, видео) обрабатываются как надо и не получают ничего. Двоичные файлы тоже — вы не превратите `.zip` в кашу по случайности. Остаётся ровно опасность: `.json`, `.css` или `.log`, который сделало видимым **Обнаружение файлов всех расширений**.

Красный побеждает там, где действовали бы оба; два кольца разом были бы просто шумом.

## Режим перемещения/переименования

Кнопка с карандашом у правого края шапки — рядом с кнопкой режима представления, того же размера, что и родные кнопки, — включает и выключает режим перемещения/переименования. Строка шапки тогда обрамляется цветом акцента, ровно как при переименовании в Файловом менеджере. Те же щелчки и клавиши теперь подтверждают перемещение или переименование через `fileManager.renameFile` Obsidian, так что все ссылки на заметку следуют за ней.

Пока идёт переименование:

- Текущее имя файла закреплено в списке каждой папки, так что переместить заметку, не переименовывая её, — один щелчок.
- Имена, уже занятые в целевой папке, приглушены, но выбрать их всё равно можно.
- Вводимое проверяется на лету по собственным правилам переименования Obsidian — те же наборы символов, те же сообщения, та же красная подсказка, что и при переименовании в дереве файлов, — так что недопустимое или конфликтующее имя отмечается по мере набора и не может быть подтверждено.
- Щелчок за пределами панели шапки или потеря шапкой фокуса завершают режим переименования.

## Одна клавиша для обоих переименований

Команда переименования (по умолчанию <kbd>F2</kbd> или та, на которую вы её переназначили) **чередует** переименование встроенного заголовка Obsidian и строку пути в шапке этого плагина с выделенным полным путём. Если вы отключили встроенный заголовок Obsidian, строка пути в шапке становится единственной целью, так что клавиша никогда не остаётся без дела.

Это работает через обёртку команды `workspace:edit-file-title`, а не перехват клавиши, поэтому переназначение сочетания и запуск команды из палитры работают без изменений.

## Как окрашиваются записи списка

| Цвет | Значит |
| --- | --- |
| **Фиолетовый** | Заметка (`.md`, `.markdown`) — то, что Obsidian откроет как заметку, выделенное из папки со смешанным содержимым |
| **Оранжевый** | Текстовый тип, для которого у Obsidian нет представления; см. [цвета предупреждений](#два-цвета-предупреждений) |
| **Приглушённый** | Вне вашего хранилища, поэтому собственная обработка хранилища не применяется |
| **Синий** | Только в режиме переименования/перемещения: запись *оставить это имя* — скорее цель, чем нечто существующее, поэтому она выделяется среди имён файлов, рядом с которыми стоит |
| **Серый** | Только в режиме переименования/перемещения: имя занято. Выбрать всё равно можно — выбор заполняет поле, где проверка отметит конфликт |

## Правила видимости

- Файлы с неподдерживаемыми расширениями появляются в списках только при включённой настройке **Обнаружение файлов всех расширений** Obsidian.
- Список показывает не более 100 записей — это собственный предел Obsidian. Когда в папке их больше, последняя строка говорит, сколько осталось за бортом; продолжайте печатать, чтобы сузить список.
- Скрытые файлы и папки появляются только при включённой настройке **Показывать скрытые файлы** этого плагина.
- **Защита от перезаписи работает одинаково независимо от видимости** — скрытый файл всё так же не даст вам его перезаписать.

## Шпаргалка

| Вы хотите… | Сделайте так |
| --- | --- |
| Открыть папку (её заметку или показать её) | Щёлкните по разделителю **после** этой папки |
| Заменить папку на соседнюю | Щёлкните по имени этой папки, затем печатайте или выбирайте |
| Переименовать заметку или перенацелить её | Щёлкните по имени заметки — вместе с расширением |
| Просмотреть содержимое папки | Щёлкните по имени этой папки; список показывает её родителя, так что щёлкайте по папке **ниже** нужной |
| Перенабрать папку и всё, что ниже | **Двойной щелчок** по имени этой папки, затем печатайте |
| Править путь начиная с папки | Щёлкните по имени этой папки, затем <kbd>End</kbd> или <kbd>→</kbd>, чтобы снять выделение |
| Перейти к файлу, набрав его путь | Щёлкните по имени файла или по пустому месту, наберите, <kbd>Enter</kbd> |
| Открыть файл в новой вкладке | <kbd>Ctrl</kbd> при выборе или <kbd>Ctrl</kbd>+<kbd>Enter</kbd> |
| Скопировать заметку куда-то вместо перемещения | Карандаш, затем <kbd>Ctrl</kbd> при выборе или подтверждении цели |
| Создать заметку по несуществующему пути | Наберите путь, <kbd>Enter</kbd>, подтвердите вопрос |
| Спуститься на уровень при наборе | Наберите `/` |
| Подняться на уровень при наборе | <kbd>Backspace</kbd> в пустом поле |
| Переместить или переименовать открытую заметку | Щёлкните по карандашу, затем просматривайте или печатайте как выше |
| Переместить без переименования | Карандаш → щелчками дойдите до целевой папки → выберите закреплённое сверху текущее имя файла |
| Переименовать на месте | <kbd>F2</kbd> дважды (первое уходит на встроенный заголовок, второе на шапку) |
| Перейти к другому хранилищу, домашней папке или диску | Щёлкните по имени хранилища |
| Открыть файл вне хранилища | Имя хранилища → выберите место → просматривайте → выберите файл (только чтение до *Править как текст*) |
| Отменить что угодно | <kbd>Esc</kbd> или щелчок за пределами панели шапки |

## Настройки

| Настройка | Значения | По умолчанию | Что делает |
| --- | --- | --- | --- |
| **Выравнивание** | По левому краю / По центру / По правому краю | По левому краю | Где путь стоит в строке шапки. *По центру* соответствует классическому виду Obsidian. |
| **Разделитель** | Любой символ | `/` | Разделитель, рисуемый между сегментами. Перед текстовым полем стоят шесть заготовок в один щелчок (`/ > ▸ › \ •`). |
| **Показывать имя хранилища** | Вкл. / Выкл. | Вкл. | Является ли само хранилище первым сегментом пути. При выключении этот сегмент становится значком 🏠, а не исчезает, так что путь по-прежнему начинается с чего-то кликабельного. |
| **Имя папки открывает список** | Вкл. / Выкл. | Вкл. | Меняет местами то, что делают имя папки и разделитель после него — см. [таблицу выше](#строка-пути). С [Folder notes](obsidian://show-plugin?id=folder-notes) разделитель открывает заметки папок. Никогда не действует в режиме переименования/перемещения. |
| **Показывать скрытые файлы** | Вкл. / Выкл. | Выкл. | Перечисляются ли скрытые файлы и папки в списках. Защита от перезаписи действует в любом случае. |
| **Доступ к внешним файлам** | Вкл. / Выкл. | **Выкл.** | Открывает ли имя хранилища список расположений. При выключении ничто в плагине никогда не смотрит за пределы этого хранилища. |

## Замена значков

Lure рисует три значка: значок корня хранилища (когда **Показывать имя хранилища** выключено), переключатель переименования/перемещения и замок, управляющий записью за пределами хранилища. Все они заменяются из темы или CSS-сниппета — задайте заменяющий глиф и спрячьте встроенный одним правилом:

```css
.lure-vault-icon {
	--lure-icon-glyph: "🏠";
	--lure-icon-svg: none;
}

.lure-rename-btn {
	--lure-icon-glyph: "✎";
	--lure-icon-svg: none;
}

/* У замка два состояния; `.is-active` — открытое. */
.lure-unlock-btn {
	--lure-icon-glyph: "🔒";
	--lure-icon-svg: none;
}

.lure-unlock-btn.is-active {
	--lure-icon-glyph: "🔓";
}
```

`--lure-icon-glyph` принимает всё, что допустимо в CSS-свойстве `content`, так что `url(...)` годится для картинки не хуже, чем текстовый глиф или эмодзи. Оставьте `--lure-icon-svg` в покое, чтобы сохранить значок Lucide и нарисовать свой глиф рядом.
